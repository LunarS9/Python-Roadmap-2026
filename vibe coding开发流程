# 小程序开发流程（Codex 协作版）

> 目标：用最少的 token 损耗、最稳定的上下文管理，迭代式地完成微信小程序开发。
> 核心思路：**把"项目记忆"从对话里搬到文件里**，让 Codex 在每个会话开始时自动读取，而不是靠你在 prompt 里反复复述。

---

## 0. 核心原理：Codex 是怎么"自动读取"上下文的

Codex CLI 在**每次会话开始、开始任何工作之前**，会按以下顺序自动查找并加载指令文件：

1. **全局层**：`~/.codex/AGENTS.md`（或 `AGENTS.override.md`，存在则优先）—— 你个人跨项目的统一编码习惯
2. **项目层**：从项目根目录（一般是 git 根目录）逐级往下走到当前工作目录，每一级目录下查找 `AGENTS.override.md` → `AGENTS.md` → 其他 fallback 文件名
3. 离当前编辑文件**最近**的 AGENTS.md 优先级最高，可以在子目录里放更细粒度的规则（比如 `pages/` 下单独放一份）

**关键认知**：AGENTS.md 只是普通 Markdown，没有固定字段要求，Codex 就是把内容当文本读进上下文。所以你写得越结构化、越少废话，省的 token 越多。

> ⚠️ 已知坑：Codex 在同一个 session 里可能不会自动重新读取被你后续修改过的 AGENTS.md（缓存问题）。**改了 AGENTS.md 后最好开新 session**，不要指望它实时生效。

---

## 1. 完整流程总览

```
Step 0  初始化仓库 + 写 AGENTS.md（一次性，长期复用）
   ↓
Step 1  产出 PROJECT.md 项目蓝图（一次性，项目级）
   ↓
Step 2  模块拆分清单（一次性，项目级）
   ↓
Step 3  单模块开发循环 ←──┐
   ↓                    │ 重复，直到所有模块完成
Step 4  验收 + 更新文档 ──┘
   ↓
Step 5  收尾：整体回归测试 + README 更新 + 提交 GitHub
```

---

## Step 0：初始化仓库，写一份「全局 + 项目」AGENTS.md

这一步只做一次（全局那份甚至可以一直复用到你以后所有小程序项目）。

### 0.1 全局 AGENTS.md（`~/.codex/AGENTS.md`）

放你**跨项目通用**的习惯，不涉及具体业务：

```markdown
# 全局工作约定（Sijing）

## 代码风格
- 微信小程序优先使用原生 Component 写法，不引入额外框架（除非项目明确要求 Taro/uni-app）
- JS 使用 ES6+，函数命名 camelCase，文件命名 kebab-case
- 注释用中文，关键逻辑必须有注释

## 协作方式
- 每次只改动我明确指定的文件，不要"顺手"重构没让你动的代码
- 改动已有文件时，只输出 diff 或修改的具体代码块，不要重新粘贴整个文件
- 涉及多文件改动前，先列出"将修改的文件清单"等我确认
- 不确定的地方直接问，不要自己猜测后直接写代码

## 测试与提交
- 改完代码后给出明确的"如何验证"步骤（怎么跑、看哪里）
- 不要自动 git commit，由我手动确认后提交
```

### 0.2 项目级 AGENTS.md（项目根目录 `/AGENTS.md`）

放**这个小程序项目**的具体技术约定，开发过程中可以持续补充：

```markdown
# 项目级约定：[小程序名称]

## 技术栈
- 微信原生小程序（wx.cloud / 自建后端，二选一注明）
- 状态管理：[全局 globalData / 自定义 store，注明]
- 网络请求：统一走 utils/request.js 封装，不要直接写 wx.request

## 目录结构
```
miniprogram/
├── pages/          # 页面
├── components/     # 自定义组件
├── utils/          # 工具函数
├── services/       # 接口请求封装
├── store/          # 全局状态（如有）
└── app.js / app.json / app.wxss
```

## 命名与路径规则
- 页面目录：`pages/模块名/模块名.js/.wxml/.wxss/.json`
- 接口函数统一放 `services/xxx.js`，导出函数名与接口语义一致（如 `getRecords`, `addRecord`）

## 当前模块状态（持续更新）
- [x] index 首页 —— 已完成
- [ ] detail 详情页 —— 进行中
- [ ] mine 我的页面 —— 未开始

## 禁止事项
- 不要修改 `app.json` 的页面注册顺序，除非我明确要求
- 不要引入未在 package.json 声明的第三方库
```

> 💡 这份文件应该**提交进 git**，跟代码一起版本管理，团队/未来的你都能直接复用上下文。

---

## Step 1：产出 PROJECT.md（项目蓝图，一次性对话产出）

这一步用**一轮较长的对话**换取后面几十轮的省力，值得"奢侈"一次。

### Prompt 模板

```
我要开发一个微信小程序：[一句话描述功能，例如"个人记账小程序"]

请帮我产出一份 PROJECT.md，包含：
1. 核心功能模块拆分（每个模块的职责边界）
2. 页面清单（路径 + 页面用途 + 主要交互）
3. 数据模型（每个核心对象的字段定义）
4. 接口清单（函数名 + 输入 + 输出，先不实现，只列规划）
5. 开发优先级排序（哪个模块先做，依据是什么）

不要现在写代码，先把蓝图定下来。
```

产出的 `PROJECT.md` 存到项目根目录，**作为后续所有模块开发的"宪法"**，每次开发新模块前可以让 Codex 先读一下这份文件确认范围。

---

## Step 2：模块拆分清单

基于 PROJECT.md，整理一份任务清单（可以是 GitHub Issues，也可以是 `TODO.md`）：

```markdown
# 开发任务清单

## P0（核心闭环）
- [ ] index 首页：本月支出汇总 + 最近记录列表
- [ ] detail 详情页：记录详情 + 编辑 + 删除
- [ ] add 添加页：新增一条记账记录

## P1（体验优化）
- [ ] mine 我的页面：个人信息 + 设置入口
- [ ] 分类统计图表

## P2（后续迭代）
- [ ] 导出报表
- [ ] 多账本切换
```

---

## Step 3：单模块开发循环（每个任务重复这个流程）

这是日常开发中**用得最频繁**的循环，每次新开一轮对话/任务时套用。

### 3.1 标准 Prompt 模板

```
任务：[一句话，范围尽量小，例如"给 detail 页加一个删除按钮"]
涉及文件：[具体路径，例如 pages/detail/detail.js, pages/detail/detail.wxml]
背景：
- 已有接口：deleteRecord(id) —— 位于 services/record.js
- 当前页面已有数据：this.data.record
约束：
- 只改这两个文件，不要动其他页面
- 用最小 diff，已有代码不要重新粘贴
验收标准：
- 点击删除按钮 → 弹出确认弹窗 → 确认后调用 deleteRecord → 成功后返回上一页
```

### 3.2 多轮迭代时的追问模板（省 token 关键）

不要重复背景，只说变化点：

```
刚才的删除按钮再加一个二次确认的 loading 状态，其他不变。
```

```
这个改动看起来可以，但 detail.wxml 里第 X 行的弹窗文案改成"确定删除这条记录吗？"
```

### 3.3 跨模块但相关时

```
现在基于刚才 detail 页的删除逻辑，给 mine 页的"清空所有记录"做同样模式的二次确认，
复用 utils/confirm-dialog.js（如果还没有就先抽出来）。
```

---

## Step 4：验收 + 更新文档

每完成一个模块：

1. **手动验证**：按 Codex 给出的验收步骤跑一遍
2. **更新项目级 AGENTS.md** 里的"当前模块状态"清单（打勾）
3. 如果这个模块引入了新的约定（比如新的工具函数、新的命名规则），**补充进 AGENTS.md**，让后续模块自动遵守

> Prompt 示例：
> ```
> detail 页已经验收通过，请把项目 AGENTS.md 里 detail 的状态改成已完成，
> 并补充一条约定：所有"删除类"操作都必须复用 utils/confirm-dialog.js
> ```

---

## Step 5：收尾

- 整体回归测试（走一遍核心用户路径）
- 更新人类可读的 `README.md`（区别于给 Codex 看的 AGENTS.md）
- `git add . && git commit -m "..."` 并推送到 GitHub
- 项目阶段性完成后，把 PROJECT.md / AGENTS.md 一起存档，作为简历项目的过程文档素材（你之前的 Jira 工具简历条目就是这么积累出来的）

---

## 附录 A：省 Token 速查表

| 场景 | 不推荐写法 | 推荐写法 |
|---|---|---|
| 描述任务 | "帮我做一个记账小程序" | "现在只做首页这一个页面：[具体范围]" |
| 提供背景 | 一大段自然语言描述项目 | 结构化列表：已有文件/已有函数/本次任务 |
| 要改动文件 | 不说明，让模型猜 | 明确列出"涉及文件" |
| 已有代码改动 | 让模型整个文件重写 | "用最小 diff，已有代码不要重新粘贴" |
| 任务边界 | 不设限制 | "不要动其他文件" + 列出验收标准 |
| 上下文延续 | 每轮重复背景 | 只说变化点，依赖 AGENTS.md 提供背景 |

## 附录 B：何时该开新 Session

- 切换到完全不相关的模块/功能
- 修改了 AGENTS.md（避免缓存问题）
- 当前会话历史已经很长，新任务不依赖之前的细节

## 附录 C：文件用途对照

| 文件 | 给谁看 | 内容定位 | 更新频率 |
|---|---|---|---|
| `~/.codex/AGENTS.md` | Codex（全局） | 跨项目通用编码习惯 | 几乎不变 |
| `/AGENTS.md`（项目根） | Codex（本项目） | 技术栈、目录结构、当前状态、禁止事项 | 每完成一个模块更新 |
| `PROJECT.md` | Codex + 你 | 项目蓝图：功能/页面/数据模型/接口规划 | 项目初期定稿，后续少改 |
| `TODO.md` | 你（也给 Codex 看） | 任务清单，跟踪进度 | 高频，每个任务勾选 |
| `README.md` | 人类（包括未来的你） | 项目介绍、安装运行方式 | 收尾阶段更新 |
